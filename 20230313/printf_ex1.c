#include <stdio.h>

int main(){
	
	//자료처리
	int area, height, width;

	width = 4;
	height = 5;
	area = width * height;

	//결과출력
	printf("면적 = %d\n", area);

	//주의할점
 //	순수한 C 컴파일러 에서는 무조건적으로 변수를 먼저 선언해주어야 한다
}
